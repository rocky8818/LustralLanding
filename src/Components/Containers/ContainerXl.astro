---
interface Props {
  padding?: boolean;
}

const { padding = false } = Astro.props;
---

<div
  class:list={[
    "container__xl",
    {
      padding: padding,
    },
  ]}
>
  <slot />
</div>

<style lang="scss">
  .container__xl {
    display: grid;
    max-width: 1440px;
    margin: 0 auto;

    &.padding {
      padding: var(--padding);
    }
  }
</style>
