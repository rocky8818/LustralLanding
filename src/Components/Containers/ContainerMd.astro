---
interface Props {
  padding?: boolean;
}

const { padding = false } = Astro.props;
---

<div
  class:list={[
    "container__md",
    {
      padding: padding,
    },
  ]}
>
  <slot />
</div>

<style lang="scss">
  .container__md {
    display: grid;
    max-width: 1056px;
    margin: 0 auto;

    &.padding {
      padding: var(--padding);
    }
  }
</style>
